using StgSharp;
using StgSharp.Math;
using System;
using System.Diagnostics;

internal class Program
{
    private unsafe static void Main(string[] args)
    {
        /*
        double time;
        TimeSpan span;
        
        
        for (int i = 0; i < 1000; i++)
        {
            Stopwatch stopwatch01 = Stopwatch.StartNew();
            stopwatch01.Start();
            for (int t = 0; t < 1000000; t++)
            {
                float a = Calc.Sqrt(t);
            }
            stopwatch01.Stop();
            span = stopwatch01.Elapsed;
            time = span.TotalMilliseconds;
            Console.WriteLine(time);

            Stopwatch stopwatch02 = new Stopwatch();
            stopwatch02.Start();
            for (int t = 0; t < 1000000; t++)
            {
                float a = MathF.Sqrt(t);
            }
            stopwatch02.Stop();
            span = stopwatch02.Elapsed;
            time = span.TotalMilliseconds;
            Console.WriteLine(time);
        
        }
        */
    }
}
    

